# Local development environment variables.
# Copy this file to .env and fill in the values.
# Never commit .env to GitHub — only this example file.

SECRET_KEY=

# Database credentials — must match docker-compose.yml
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=db
DB_PORT=5432